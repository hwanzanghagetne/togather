spring:
  application:
    name: chat
  profiles:
    active: dev  # 기본값. 배포 시 prod로 변경

jwt:
  secret: ${JWT_SECRET}
  access-expiration: 1800000    # 30분 (ms)
  refresh-expiration: 604800000 # 7일 (ms)

